# Data Directory

- `raw/`: Raw data downloaded from the competition source.
- `processed/`: Data after cleaning, normalization, and standardizing.
- `metadata/`: Extracted metadata from tables (headers, unit types, schemas).
- `indexes/`: Built FAISS and BM25 indexes for retrieval.
- `submission/`: Final generated CSV/JSON/ZIP files for submission.
